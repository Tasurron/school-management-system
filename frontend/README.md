# Frontend — School Management System

This is the Next.js frontend for the School Management System.
See the [project root README](../README.md) for the full setup guide, demo credentials, and architecture overview.

Quick start (backend must already be running on `http://localhost:5047`):

```bash
npm install
cp .env.local.example .env.local
npm run dev
```
