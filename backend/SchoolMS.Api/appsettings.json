{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_comment": "These are placeholder values for a committed config file. Real local development values (DB connection string, JWT key) live in appsettings.Development.json, which is NOT gitignored because it only holds a throwaway local dev DB password, not a real secret. In a real deployment, production values would come from environment variables or a secret store instead of this file.",
  "ConnectionStrings": {
    "DefaultConnection": "Host=CHANGE_ME;Port=5432;Database=CHANGE_ME;Username=CHANGE_ME;Password=CHANGE_ME"
  },
  "Jwt": {
    "Key": "CHANGE_ME_REPLACE_WITH_A_REAL_SECRET_KEY",
    "Issuer": "SchoolMS",
    "Audience": "SchoolMSClient",
    "ExpiryMinutes": 60
  }
}
